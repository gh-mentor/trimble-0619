# This file contains git commands to stage, commit, and push changes to the main branch remote repository. 

# Stage all changes

git add .

# Commit changes

git commit -m "Update"

# Push changes to the main branch remote repository

git push origin main